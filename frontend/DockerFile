FROM node:22-alpine

WORKDIR /app

# package-lock.json は npm install 前は存在しないため * でオプショナルにする
COPY package.json package-lock.json* ./
RUN npm install

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
